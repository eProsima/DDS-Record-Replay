# Allowed topics
allowlist:
  - name: "*"

# Blocked topics
blocklist:
  - name: "add_blocked_topics_list_here"

# Domain ID
dds:
  domain: 0

# Recorder output file
recorder:
  output:
    extension: ".mcap"
    filename: "output"
    use-timestamp: true
    path: "."
