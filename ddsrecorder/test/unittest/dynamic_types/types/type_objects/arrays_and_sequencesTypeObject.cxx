// Copyright 2016 Proyectos y Sistemas de Mantenimiento SL (eProsima).
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

/*!
 * @file arrays_and_sequencesTypeObject.cpp
 * This source file contains the definition of the described types in the IDL file.
 *
 * This file was generated by the tool gen.
 */

#ifdef _WIN32
// Remove linker warning LNK4221 on Visual Studio
namespace { char dummy; }
#endif

#include "arrays_and_sequences.h"
#include "arrays_and_sequencesTypeObject.h"
#include <utility>
#include <sstream>
#include <fastrtps/rtps/common/SerializedPayload.h>
#include <fastrtps/utils/md5.h>
#include <fastrtps/types/TypeObjectFactory.h>
#include <fastrtps/types/TypeNamesGenerator.h>
#include <fastrtps/types/AnnotationParameterValue.h>
#include <fastcdr/FastBuffer.h>
#include <fastcdr/Cdr.h>

using namespace eprosima::fastrtps::rtps;

void registerarrays_and_sequencesTypes()
{
    TypeObjectFactory *factory = TypeObjectFactory::get_instance();
    factory->add_type_object("AnInternalObject", GetAnInternalObjectIdentifier(true),
    GetAnInternalObjectObject(true));
    factory->add_type_object("AnInternalObject", GetAnInternalObjectIdentifier(false),
    GetAnInternalObjectObject(false));

    factory->add_type_object("arrays_and_sequences", Getarrays_and_sequencesIdentifier(true),
    Getarrays_and_sequencesObject(true));
    factory->add_type_object("arrays_and_sequences", Getarrays_and_sequencesIdentifier(false),
    Getarrays_and_sequencesObject(false));

}

const TypeIdentifier* GetAnInternalObjectIdentifier(bool complete)
{
    const TypeIdentifier * c_identifier = TypeObjectFactory::get_instance()->get_type_identifier("AnInternalObject", complete);
    if (c_identifier != nullptr && (!complete || c_identifier->_d() == EK_COMPLETE))
    {
        return c_identifier;
    }

    GetAnInternalObjectObject(complete); // Generated inside
    return TypeObjectFactory::get_instance()->get_type_identifier("AnInternalObject", complete);
}

const TypeObject* GetAnInternalObjectObject(bool complete)
{
    const TypeObject* c_type_object = TypeObjectFactory::get_instance()->get_type_object("AnInternalObject", complete);
    if (c_type_object != nullptr)
    {
        return c_type_object;
    }
    else if (complete)
    {
        return GetCompleteAnInternalObjectObject();
    }
    //else
    return GetMinimalAnInternalObjectObject();
}

const TypeObject* GetMinimalAnInternalObjectObject()
{
    const TypeObject* c_type_object = TypeObjectFactory::get_instance()->get_type_object("AnInternalObject", false);
    if (c_type_object != nullptr)
    {
        return c_type_object;
    }

    TypeObject *type_object = new TypeObject();
    type_object->_d(EK_MINIMAL);
    type_object->minimal()._d(TK_STRUCTURE);

    type_object->minimal().struct_type().struct_flags().IS_FINAL(false);
    type_object->minimal().struct_type().struct_flags().IS_APPENDABLE(false);
    type_object->minimal().struct_type().struct_flags().IS_MUTABLE(false);
    type_object->minimal().struct_type().struct_flags().IS_NESTED(false);
    type_object->minimal().struct_type().struct_flags().IS_AUTOID_HASH(false); // Unsupported

    MemberId memberId = 0;
    MinimalStructMember mst_x;
    mst_x.common().member_id(memberId++);
    mst_x.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    mst_x.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    mst_x.common().member_flags().IS_EXTERNAL(false); // Unsupported
    mst_x.common().member_flags().IS_OPTIONAL(false);
    mst_x.common().member_flags().IS_MUST_UNDERSTAND(false);
    mst_x.common().member_flags().IS_KEY(false);
    mst_x.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    mst_x.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("float", false));

    MD5 x_hash("x");
    for(int i = 0; i < 4; ++i)
    {
        mst_x.detail().name_hash()[i] = x_hash.digest[i];
    }
    type_object->minimal().struct_type().member_seq().emplace_back(mst_x);

    MinimalStructMember mst_positive;
    mst_positive.common().member_id(memberId++);
    mst_positive.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    mst_positive.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    mst_positive.common().member_flags().IS_EXTERNAL(false); // Unsupported
    mst_positive.common().member_flags().IS_OPTIONAL(false);
    mst_positive.common().member_flags().IS_MUST_UNDERSTAND(false);
    mst_positive.common().member_flags().IS_KEY(false);
    mst_positive.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    mst_positive.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("bool", false));

    MD5 positive_hash("positive");
    for(int i = 0; i < 4; ++i)
    {
        mst_positive.detail().name_hash()[i] = positive_hash.digest[i];
    }
    type_object->minimal().struct_type().member_seq().emplace_back(mst_positive);


    // Header
    // TODO Inheritance
    //type_object->minimal().struct_type().header().base_type()._d(EK_MINIMAL);
    //type_object->minimal().struct_type().header().base_type().equivalence_hash()[0..13];

    TypeIdentifier identifier;
    identifier._d(EK_MINIMAL);

    SerializedPayload_t payload(static_cast<uint32_t>(
        MinimalStructType::getCdrSerializedSize(type_object->minimal().struct_type()) + 4));
    eprosima::fastcdr::FastBuffer fastbuffer((char*) payload.data, payload.max_size);
    // Fixed endian (Page 221, EquivalenceHash definition of Extensible and Dynamic Topic Types for DDS document)
    eprosima::fastcdr::Cdr ser(
        fastbuffer, eprosima::fastcdr::Cdr::LITTLE_ENDIANNESS,
        eprosima::fastcdr::Cdr::DDS_CDR); // Object that serializes the data.
    payload.encapsulation = CDR_LE;

    type_object->serialize(ser);
    payload.length = (uint32_t)ser.getSerializedDataLength(); //Get the serialized length
    MD5 objectHash;
    objectHash.update((char*)payload.data, payload.length);
    objectHash.finalize();
    for(int i = 0; i < 14; ++i)
    {
        identifier.equivalence_hash()[i] = objectHash.digest[i];
    }

    TypeObjectFactory::get_instance()->add_type_object("AnInternalObject", &identifier, type_object);
    delete type_object;
    return TypeObjectFactory::get_instance()->get_type_object("AnInternalObject", false);
}

const TypeObject* GetCompleteAnInternalObjectObject()
{
    const TypeObject* c_type_object = TypeObjectFactory::get_instance()->get_type_object("AnInternalObject", true);
    if (c_type_object != nullptr && c_type_object->_d() == EK_COMPLETE)
    {
        return c_type_object;
    }

    TypeObject *type_object = new TypeObject();
    type_object->_d(EK_COMPLETE);
    type_object->complete()._d(TK_STRUCTURE);

    type_object->complete().struct_type().struct_flags().IS_FINAL(false);
    type_object->complete().struct_type().struct_flags().IS_APPENDABLE(false);
    type_object->complete().struct_type().struct_flags().IS_MUTABLE(false);
    type_object->complete().struct_type().struct_flags().IS_NESTED(false);
    type_object->complete().struct_type().struct_flags().IS_AUTOID_HASH(false); // Unsupported

    MemberId memberId = 0;
    CompleteStructMember cst_x;
    cst_x.common().member_id(memberId++);
    cst_x.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    cst_x.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    cst_x.common().member_flags().IS_EXTERNAL(false); // Unsupported
    cst_x.common().member_flags().IS_OPTIONAL(false);
    cst_x.common().member_flags().IS_MUST_UNDERSTAND(false);
    cst_x.common().member_flags().IS_KEY(false);
    cst_x.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    cst_x.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("float", false));

    cst_x.detail().name("x");

    type_object->complete().struct_type().member_seq().emplace_back(cst_x);

    CompleteStructMember cst_positive;
    cst_positive.common().member_id(memberId++);
    cst_positive.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    cst_positive.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    cst_positive.common().member_flags().IS_EXTERNAL(false); // Unsupported
    cst_positive.common().member_flags().IS_OPTIONAL(false);
    cst_positive.common().member_flags().IS_MUST_UNDERSTAND(false);
    cst_positive.common().member_flags().IS_KEY(false);
    cst_positive.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    cst_positive.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("bool", false));

    cst_positive.detail().name("positive");

    type_object->complete().struct_type().member_seq().emplace_back(cst_positive);


    // Header
    type_object->complete().struct_type().header().detail().type_name("AnInternalObject");
    // TODO inheritance


    TypeIdentifier identifier;
    identifier._d(EK_COMPLETE);

    SerializedPayload_t payload(static_cast<uint32_t>(
        CompleteStructType::getCdrSerializedSize(type_object->complete().struct_type()) + 4));
    eprosima::fastcdr::FastBuffer fastbuffer((char*) payload.data, payload.max_size);
    // Fixed endian (Page 221, EquivalenceHash definition of Extensible and Dynamic Topic Types for DDS document)
    eprosima::fastcdr::Cdr ser(
        fastbuffer, eprosima::fastcdr::Cdr::LITTLE_ENDIANNESS,
        eprosima::fastcdr::Cdr::DDS_CDR); // Object that serializes the data.
    payload.encapsulation = CDR_LE;

    type_object->serialize(ser);
    payload.length = (uint32_t)ser.getSerializedDataLength(); //Get the serialized length
    MD5 objectHash;
    objectHash.update((char*)payload.data, payload.length);
    objectHash.finalize();
    for(int i = 0; i < 14; ++i)
    {
        identifier.equivalence_hash()[i] = objectHash.digest[i];
    }

    TypeObjectFactory::get_instance()->add_type_object("AnInternalObject", &identifier, type_object);
    delete type_object;
    return TypeObjectFactory::get_instance()->get_type_object("AnInternalObject", true);
}

const TypeIdentifier* Getarrays_and_sequencesIdentifier(bool complete)
{
    const TypeIdentifier * c_identifier = TypeObjectFactory::get_instance()->get_type_identifier("arrays_and_sequences", complete);
    if (c_identifier != nullptr && (!complete || c_identifier->_d() == EK_COMPLETE))
    {
        return c_identifier;
    }

    Getarrays_and_sequencesObject(complete); // Generated inside
    return TypeObjectFactory::get_instance()->get_type_identifier("arrays_and_sequences", complete);
}

const TypeObject* Getarrays_and_sequencesObject(bool complete)
{
    const TypeObject* c_type_object = TypeObjectFactory::get_instance()->get_type_object("arrays_and_sequences", complete);
    if (c_type_object != nullptr)
    {
        return c_type_object;
    }
    else if (complete)
    {
        return GetCompletearrays_and_sequencesObject();
    }
    //else
    return GetMinimalarrays_and_sequencesObject();
}

const TypeObject* GetMinimalarrays_and_sequencesObject()
{
    const TypeObject* c_type_object = TypeObjectFactory::get_instance()->get_type_object("arrays_and_sequences", false);
    if (c_type_object != nullptr)
    {
        return c_type_object;
    }

    TypeObject *type_object = new TypeObject();
    type_object->_d(EK_MINIMAL);
    type_object->minimal()._d(TK_STRUCTURE);

    type_object->minimal().struct_type().struct_flags().IS_FINAL(false);
    type_object->minimal().struct_type().struct_flags().IS_APPENDABLE(false);
    type_object->minimal().struct_type().struct_flags().IS_MUTABLE(false);
    type_object->minimal().struct_type().struct_flags().IS_NESTED(false);
    type_object->minimal().struct_type().struct_flags().IS_AUTOID_HASH(false); // Unsupported

    MemberId memberId = 0;
    MinimalStructMember mst_unlimited_vector;
    mst_unlimited_vector.common().member_id(memberId++);
    mst_unlimited_vector.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    mst_unlimited_vector.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    mst_unlimited_vector.common().member_flags().IS_EXTERNAL(false); // Unsupported
    mst_unlimited_vector.common().member_flags().IS_OPTIONAL(false);
    mst_unlimited_vector.common().member_flags().IS_MUST_UNDERSTAND(false);
    mst_unlimited_vector.common().member_flags().IS_KEY(false);
    mst_unlimited_vector.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    mst_unlimited_vector.common().member_type_id(*TypeObjectFactory::get_instance()->get_sequence_identifier("AnInternalObject", 0, false));


    MD5 unlimited_vector_hash("unlimited_vector");
    for(int i = 0; i < 4; ++i)
    {
        mst_unlimited_vector.detail().name_hash()[i] = unlimited_vector_hash.digest[i];
    }
    type_object->minimal().struct_type().member_seq().emplace_back(mst_unlimited_vector);

    MinimalStructMember mst_limited_vector;
    mst_limited_vector.common().member_id(memberId++);
    mst_limited_vector.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    mst_limited_vector.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    mst_limited_vector.common().member_flags().IS_EXTERNAL(false); // Unsupported
    mst_limited_vector.common().member_flags().IS_OPTIONAL(false);
    mst_limited_vector.common().member_flags().IS_MUST_UNDERSTAND(false);
    mst_limited_vector.common().member_flags().IS_KEY(false);
    mst_limited_vector.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    mst_limited_vector.common().member_type_id(*TypeObjectFactory::get_instance()->get_sequence_identifier("AnInternalObject", 10, false));


    MD5 limited_vector_hash("limited_vector");
    for(int i = 0; i < 4; ++i)
    {
        mst_limited_vector.detail().name_hash()[i] = limited_vector_hash.digest[i];
    }
    type_object->minimal().struct_type().member_seq().emplace_back(mst_limited_vector);

    MinimalStructMember mst_limited_array;
    mst_limited_array.common().member_id(memberId++);
    mst_limited_array.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    mst_limited_array.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    mst_limited_array.common().member_flags().IS_EXTERNAL(false); // Unsupported
    mst_limited_array.common().member_flags().IS_OPTIONAL(false);
    mst_limited_array.common().member_flags().IS_MUST_UNDERSTAND(false);
    mst_limited_array.common().member_flags().IS_KEY(false);
    mst_limited_array.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    mst_limited_array.common().member_type_id(*TypeObjectFactory::get_instance()->get_array_identifier("AnInternalObject", {10}, false));


    MD5 limited_array_hash("limited_array");
    for(int i = 0; i < 4; ++i)
    {
        mst_limited_array.detail().name_hash()[i] = limited_array_hash.digest[i];
    }
    type_object->minimal().struct_type().member_seq().emplace_back(mst_limited_array);


    // Header
    // TODO Inheritance
    //type_object->minimal().struct_type().header().base_type()._d(EK_MINIMAL);
    //type_object->minimal().struct_type().header().base_type().equivalence_hash()[0..13];

    TypeIdentifier identifier;
    identifier._d(EK_MINIMAL);

    SerializedPayload_t payload(static_cast<uint32_t>(
        MinimalStructType::getCdrSerializedSize(type_object->minimal().struct_type()) + 4));
    eprosima::fastcdr::FastBuffer fastbuffer((char*) payload.data, payload.max_size);
    // Fixed endian (Page 221, EquivalenceHash definition of Extensible and Dynamic Topic Types for DDS document)
    eprosima::fastcdr::Cdr ser(
        fastbuffer, eprosima::fastcdr::Cdr::LITTLE_ENDIANNESS,
        eprosima::fastcdr::Cdr::DDS_CDR); // Object that serializes the data.
    payload.encapsulation = CDR_LE;

    type_object->serialize(ser);
    payload.length = (uint32_t)ser.getSerializedDataLength(); //Get the serialized length
    MD5 objectHash;
    objectHash.update((char*)payload.data, payload.length);
    objectHash.finalize();
    for(int i = 0; i < 14; ++i)
    {
        identifier.equivalence_hash()[i] = objectHash.digest[i];
    }

    TypeObjectFactory::get_instance()->add_type_object("arrays_and_sequences", &identifier, type_object);
    delete type_object;
    return TypeObjectFactory::get_instance()->get_type_object("arrays_and_sequences", false);
}

const TypeObject* GetCompletearrays_and_sequencesObject()
{
    const TypeObject* c_type_object = TypeObjectFactory::get_instance()->get_type_object("arrays_and_sequences", true);
    if (c_type_object != nullptr && c_type_object->_d() == EK_COMPLETE)
    {
        return c_type_object;
    }

    TypeObject *type_object = new TypeObject();
    type_object->_d(EK_COMPLETE);
    type_object->complete()._d(TK_STRUCTURE);

    type_object->complete().struct_type().struct_flags().IS_FINAL(false);
    type_object->complete().struct_type().struct_flags().IS_APPENDABLE(false);
    type_object->complete().struct_type().struct_flags().IS_MUTABLE(false);
    type_object->complete().struct_type().struct_flags().IS_NESTED(false);
    type_object->complete().struct_type().struct_flags().IS_AUTOID_HASH(false); // Unsupported

    MemberId memberId = 0;
    CompleteStructMember cst_unlimited_vector;
    cst_unlimited_vector.common().member_id(memberId++);
    cst_unlimited_vector.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    cst_unlimited_vector.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    cst_unlimited_vector.common().member_flags().IS_EXTERNAL(false); // Unsupported
    cst_unlimited_vector.common().member_flags().IS_OPTIONAL(false);
    cst_unlimited_vector.common().member_flags().IS_MUST_UNDERSTAND(false);
    cst_unlimited_vector.common().member_flags().IS_KEY(false);
    cst_unlimited_vector.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    cst_unlimited_vector.common().member_type_id(*TypeObjectFactory::get_instance()->get_sequence_identifier("AnInternalObject", 0, true));


    cst_unlimited_vector.detail().name("unlimited_vector");

    type_object->complete().struct_type().member_seq().emplace_back(cst_unlimited_vector);

    CompleteStructMember cst_limited_vector;
    cst_limited_vector.common().member_id(memberId++);
    cst_limited_vector.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    cst_limited_vector.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    cst_limited_vector.common().member_flags().IS_EXTERNAL(false); // Unsupported
    cst_limited_vector.common().member_flags().IS_OPTIONAL(false);
    cst_limited_vector.common().member_flags().IS_MUST_UNDERSTAND(false);
    cst_limited_vector.common().member_flags().IS_KEY(false);
    cst_limited_vector.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    cst_limited_vector.common().member_type_id(*TypeObjectFactory::get_instance()->get_sequence_identifier("AnInternalObject", 10, true));


    cst_limited_vector.detail().name("limited_vector");

    type_object->complete().struct_type().member_seq().emplace_back(cst_limited_vector);

    CompleteStructMember cst_limited_array;
    cst_limited_array.common().member_id(memberId++);
    cst_limited_array.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    cst_limited_array.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    cst_limited_array.common().member_flags().IS_EXTERNAL(false); // Unsupported
    cst_limited_array.common().member_flags().IS_OPTIONAL(false);
    cst_limited_array.common().member_flags().IS_MUST_UNDERSTAND(false);
    cst_limited_array.common().member_flags().IS_KEY(false);
    cst_limited_array.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    cst_limited_array.common().member_type_id(*TypeObjectFactory::get_instance()->get_array_identifier("AnInternalObject", {10}, true));


    cst_limited_array.detail().name("limited_array");

    type_object->complete().struct_type().member_seq().emplace_back(cst_limited_array);


    // Header
    type_object->complete().struct_type().header().detail().type_name("arrays_and_sequences");
    // TODO inheritance


    TypeIdentifier identifier;
    identifier._d(EK_COMPLETE);

    SerializedPayload_t payload(static_cast<uint32_t>(
        CompleteStructType::getCdrSerializedSize(type_object->complete().struct_type()) + 4));
    eprosima::fastcdr::FastBuffer fastbuffer((char*) payload.data, payload.max_size);
    // Fixed endian (Page 221, EquivalenceHash definition of Extensible and Dynamic Topic Types for DDS document)
    eprosima::fastcdr::Cdr ser(
        fastbuffer, eprosima::fastcdr::Cdr::LITTLE_ENDIANNESS,
        eprosima::fastcdr::Cdr::DDS_CDR); // Object that serializes the data.
    payload.encapsulation = CDR_LE;

    type_object->serialize(ser);
    payload.length = (uint32_t)ser.getSerializedDataLength(); //Get the serialized length
    MD5 objectHash;
    objectHash.update((char*)payload.data, payload.length);
    objectHash.finalize();
    for(int i = 0; i < 14; ++i)
    {
        identifier.equivalence_hash()[i] = objectHash.digest[i];
    }

    TypeObjectFactory::get_instance()->add_type_object("arrays_and_sequences", &identifier, type_object);
    delete type_object;
    return TypeObjectFactory::get_instance()->get_type_object("arrays_and_sequences", true);
}
