.. include:: ../../exports/alias.include

.. _replayer_getting_started_usage_example:

Replayer first workflow
#######################

The former Replayer-only example is now the second half of the validated
:ref:`record_replay_quickstart`. It uses the exact timestamped file created by
Recorder and verifies the replay with a ShapesDemo subscriber.
